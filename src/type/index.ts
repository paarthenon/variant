import {variantCosmos} from '../cosmos';

export const {
    descope,
    flags,
    inferTypes,
    isOfVariant,
    isType,
    match,
    matcher,
    ofLiteral,
    onLiteral,
    prematch,
    remote,
    scopedVariant,
    sequence,
    typed,
    types,
    variant,
    variantList,
    variantModule,
    variation,
} = variantCosmos({key: 'type'});
