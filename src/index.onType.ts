import {variantCosmos} from './cosmos';

export const {
    descope,
    flags,
    isOfVariant,
    isType,
    match,
    onLiteral,
    remote,
    scopedVariant,
    sequence,
    typed,
    types,
    variant,
    variantList,
    variantModule,
    variation,
} = variantCosmos({key: 'type'});
